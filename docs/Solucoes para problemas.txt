1) Se ocorrer uma exceção do tipo OutOfMemoryException
   a) Abra o arquivo bin/XDocumentServer_core.sh
   b) Edite a variável de ambiente XMX colocando um valor maior de Gigabytes no formato XMX=?G onde ? é o número de gigabytes.